on: push

jobs:
  eslint-check:
    name: 🤔 ESLint Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Run ESLint
        run: yarn eslint
